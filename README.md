Sniffix is a port scanner which detects the devices/servers connected to the network.

It uses scapy to push an ARP scan across the network.

The UI let's you choose which device to scan (vigerous open ports scan / service detection / device|server info)
